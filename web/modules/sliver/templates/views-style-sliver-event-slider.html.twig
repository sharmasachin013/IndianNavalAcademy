{#
/**
 * @file
 * Default theme implementation for a view template to display a list of rows.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 *
 * @see template_preprocess_views_style_sliver_event_slider()
 */
#}
    <div class="highlights-column right-column">
      <div class="column-header">
        <h3>Recent Events</h3>
      </div>
    
      <div class="event-slider" id="event-slider">
    
        <button class="prev-event" onclick="changeEventSlide(-1)">&#10094;</button>
        <button class="next-event" onclick="changeEventSlide(1)">&#10095;</button>
    {% for row in rows %}
        <div class="event-slide {% if loop.first %}active{% endif %}">
          {{ row.url }}
          <img src="{{ row.url }}" alt="Annual Regatta">
          <h4 class="event-title" onclick="openContentModal('Annual Regatta'); return false;">Annual Regatta 2025:
            Watermanship Showcase</h4>
        </div>
        {% endfor %}
    
  
    
   
      </div>
    </div>
